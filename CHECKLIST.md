# CHECKLIST

이 문서는 작업 완료 여부를 검증하기 위한 체크리스트이다.  
각 구현 작업이 끝날 때마다 관련 항목을 확인하고 필요한 경우 결과를 기록한다.

## 공통 검토 기준

- [ ] `README.md`의 1차 MVP 범위를 벗어나지 않았다.
- [ ] 범위 밖 기능은 별도 변경요청으로 분리했다.
- [ ] 책임이 한 계층이나 한 모듈에 과도하게 섞이지 않았다.
- [ ] 상위 계층 구조와 하위 역할 모듈 구조가 충돌하지 않는다.
- [ ] 비즈니스 규칙은 Domain 계층에 위치한다.
- [ ] 키워드 판정과 도메인 분류 규칙은 Domain 계층에 위치한다.
- [ ] 원천 응답 정규화 책임은 Infrastructure 또는 Application 경계에 위치한다.
- [ ] 외부 API, DB, 파일 시스템 의존성은 Infrastructure 계층으로 분리했다.
- [ ] 하드코딩해야 할 값은 설정으로 분리했다.
- [ ] 동일 작업을 재실행해도 결과가 깨지지 않는다.
- [ ] 새 소스 추가가 기존 핵심 로직 수정 없이 가능하다.

## 구현 전 체크리스트

- [x] 공통 데이터 모델이 확정되었다.
- [ ] 설정 파일 구조가 확정되었다.
- [x] 중복 판단 키가 확정되었다.
- [ ] 키워드 판정 규칙이 확정되었다.
- [ ] 로그 필수 항목이 확정되었다.
- [ ] 예외 처리 정책이 확정되었다.
- [ ] 엑셀 시트/컬럼 기준이 확정되었다.
- [ ] 테스트 범위가 확정되었다.

## 데이터 모델 체크리스트

- [x] `notice_type`은 `support`, `bid`, `rnd`, `startup` 기준과 충돌하지 않는다.
- [x] `business_domains`는 다중 선택을 지원한다.
- [x] `primary_domain`은 대표 1개만 가진다.
- [x] `match_keywords`는 매칭된 키워드 목록을 보존한다.
- [x] 날짜는 내부에서 일관된 타입으로 관리한다.
- [x] 엑셀 출력 날짜는 `YYYY-MM-DD` 형식으로 변환 가능하다.
- [x] URL은 클릭 가능한 값으로 검증 가능하다.
- [x] 중복 판단 키를 안정적으로 생성할 수 있다.

## 설정 체크리스트

- [ ] 키워드 목록을 코드에 하드코딩하지 않았다.
- [ ] 시트명을 코드에 하드코딩하지 않았다.
- [ ] 파일명 규칙을 코드에 하드코딩하지 않았다.
- [ ] 활성 소스 목록을 코드에 하드코딩하지 않았다.
- [ ] 재시도 횟수를 코드에 하드코딩하지 않았다.
- [ ] 타임아웃 값을 코드에 하드코딩하지 않았다.
- [ ] 출력 경로를 코드에 하드코딩하지 않았다.
- [ ] 필수 설정값은 실행 시작 시 검증된다.

## 수집 체크리스트

- [ ] 기업마당 조회 인터페이스가 정의되었다.
- [ ] 나라장터 조회 인터페이스가 정의되었다.
- [ ] 기업마당 샘플 응답 fixture가 확보되었다.
- [ ] 나라장터 샘플 응답 fixture가 확보되었다.
- [ ] 외부 API DTO와 내부 `Notice` 모델이 분리되었다.
- [ ] 샘플 응답 fixture 기반으로 DTO 파싱을 검증할 수 있다.
- [ ] 외부 API 없이 핵심 정규화 로직을 검증할 수 있다.
- [ ] 외부 API 점검이나 공지성 중단을 내부 장애와 구분한다.
- [ ] 일부 선택 필드 누락은 비치명적 오류로 처리 가능하다.
- [ ] 한 트랙 전체 수집 실패는 치명적 실패로 처리 가능하다.

## 키워드 판정 체크리스트

- [ ] 핵심 키워드 매칭이 동작한다.
- [ ] 보조 키워드 매칭이 동작한다.
- [ ] 강제 제외 키워드 매칭이 동작한다.
- [ ] 강제 제외 키워드는 적격 판정에서 우선 처리된다.
- [ ] 매칭된 키워드가 `match_keywords`에 기록된다.
- [ ] `business_domains`와 `primary_domain`이 일관되게 산출된다.

## 저장 체크리스트

- [ ] 적격 판정된 공고는 저장된다.
- [ ] 동일 건 재수집 시 중복 저장되지 않는다.
- [ ] 저장 실패는 치명적 오류로 처리된다.
- [ ] 저장 결과 건수가 로그에 기록된다.
- [ ] 저장소 접근은 저장소 계층으로 통일된다.

## 엑셀 산출물 체크리스트

- [ ] `support_notices` 시트가 생성된다.
- [ ] `bid_notices` 시트가 생성된다.
- [ ] 필수 컬럼이 모두 포함된다.
- [ ] 날짜 형식은 `YYYY-MM-DD`이다.
- [ ] URL은 클릭 가능하다.
- [ ] 최근 공고를 5분 이내 검토 가능한 가독성을 갖춘다.

## 로그 체크리스트

- [ ] 실행 시작 시각이 기록된다.
- [ ] 실행 종료 시각이 기록된다.
- [ ] 실행 ID가 기록된다.
- [ ] 실행 액션이 기록된다.
- [ ] 실행 결과 상태가 기록된다.
- [ ] 소스명이 기록된다.
- [ ] 조회 건수가 기록된다.
- [ ] 저장 건수가 기록된다.
- [ ] 제외 건수가 기록된다.
- [ ] 오류 유형이 기록된다.
- [ ] 출력 파일 경로가 기록된다.

## 테스트 체크리스트

- [ ] 키워드 판정 단위 테스트가 있다.
- [ ] 날짜 변환 단위 테스트가 있다.
- [ ] 중복 키 생성 단위 테스트가 있다.
- [ ] 정규화 단위 테스트가 있다.
- [ ] fixture 기반 정규화 테스트가 있다.
- [ ] 외부 API 없이 테스트를 실행할 수 있다.
- [ ] 조회에서 저장까지 통합 테스트가 있다.
- [ ] 엑셀 시트/컬럼 생성 테스트가 있다.
- [ ] format 검사를 실행했다.
- [ ] lint 검사를 실행했다.
- [ ] type check를 실행했다.
